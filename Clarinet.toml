[project]
name = "compress-client"
authors = ["DecentralizedCompression Team"]
description = "Decentralized client compression resource management"
telemetry = false
requirements = []
[contracts.compression-registry]
path = "contracts/compression-registry.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
